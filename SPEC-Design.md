# Cloudfin 设计文档

**版本：** v1.0
**日期：** 2026-04-05
**状态：** Draft（待评审）
**级别：** L0（纲领性）

---

## 1. 总体设计

### 1.1 项目愿景

Cloudfin 是一个**模块化跨平台通信与同步框架**。

类比目标：

- **Redis** —— 纯 API/CLI 控制，程序化驱动
- **无头浏览器** —— 自动化脚本接管一切

Cloudfin 不做封闭生态。**核心目标：让任意多个设备在任意网络环境下安全、低延迟地同步数据。**

UI 与 Core/Modules 严格分离：UI 是独立进程，可以是任意平台的原生应用，也可以是 CLI 工具。

一切围绕这个目标展开：数据怎么存、怎么传、怎么一致、怎么扩展，都服务于这个核心承诺。

### 1.2 五层架构总览

```
┌─────────────────────────────────────────────────────┐
│  第一层：UI 层（平台原生，用户体验优先）                  │
│  Android App / iOS App / Qt / CLI …                  │
└─────────────────────────┬───────────────────────────┘
                          │ WebSocket（单一协议）
┌─────────────────────────▼───────────────────────────┐
│  第二层：Core 层（跨平台，纯逻辑，无 UI）                │
│  连接管理 / 路由 / 模块调度 / 配置                       │
└─────────────────────────┬───────────────────────────┘
                          │ 统一接口（Core API）
┌─────────────────────────▼───────────────────────────┐
│  第三层：通信层 Modules（地基/桥梁，网络通路）              │
│  p2p（点对点）/ tor（洋葱路由）/ i2p（隧道）              │
└─────────────────────────────────────────────────────┘
                          ↑ 三层都只和 Core 通信，
                          │ 模块之间不直接通信
┌─────────────────────────▼───────────────────────────┐
│  第四层：加密层 Modules（装修/安全设施）                    │
│  crypto（端到端加密管道）                                │
└─────────────────────────────────────────────────────┘
                          ↑
┌─────────────────────────▼───────────────────────────┐
│  第五层：同步层 Modules（人工作/车通行，核心价值）           │
│  crdt（文档协同）/ storage（持久化存储）                   │
└─────────────────────────────────────────────────────┘
```

三层 Modules 都**只和 Core 通信**，彼此之间不直接通信。分三层纯粹是**功能分类**：

- **通信层** = 地基 + 桥梁，网络通路本身，提供节点互联能力
- **加密层** = 装修 + 安全设施，在通信基础上加一层保护
- **同步层** = 人在工作 + 车在通行，真正有价值的业务在这一层发生

五层各司其职，任一层换语言、换框架，其他层**无需修改**。

### 1.3 核心功能需求

| 需求 | 说明 |
|------|------|
| 跨平台守护进程 | Android / iOS / Windows / macOS / Linux，后台常驻，开机自启 |
| 模块化插件系统 | .so/.dylib/.dll，可按需安装、加载、卸载，与 Core 解耦 |
| UI与Core分离 | 独立进程，WebSocket 单一连接，可选任意技术栈 |
| P2P组网 | 去中心化、NAT穿透、端到端加密 |
| CRDT文档同步 | 最终一致性、离线可用、冲突自动合并 |

### 1.4 基本技术决策

| 问题 | 答案 |
|------|------|
| 为什么用 Rust | 性能优先 + 内存安全 + 跨平台 FFI |
| 为什么五层分离 | 解耦，各自演进，互不影响 |
| 为什么选择 Open Core | 基础开源（AGPL）+ 高级商业 + 防垄断 |

---

## 2. UI 设计

### 2.1 左侧栏导航

UI 左侧栏分为五个固定分区，按从上到下顺序：

| 顺序 | 分区 | 内容 |
|------|------|------|
| 1 | 核心 | Core 运行状态、安装管理 |
| 2 | 通信 | 通信层 Modules（p2p / tor / i2p）|
| 3 | 加密 | 加密层 Modules（crypto）|
| 4 | 同步 | 同步层 Modules（crdt / storage）|
| 5 | 设置 | UI 信息、外观设置 |

### 2.2 核心页面

**已安装状态：**

```
┌──────────────────────────────────────┐
│ [核心图标]  Cloudfin Core            │
│  版本: v1.0.0  状态: 运行中          │
│                                      │
│  ● 已连接  端口: 19001               │
│                                      │
│  ─────────────────────────────────   │
│                                      │
│  CPU: xxx MB  内存: xxx MB          │
│  运行时间: x天 x小时                  │
│                                      │
│  [停止核心]  [重启核心]               │
└──────────────────────────────────────┘
```

**未安装状态：**

```
┌──────────────────────────────────────┐
│ [核心图标]  Cloudfin Core            │
│                                      │
│  ⚠️ 核心未安装                       │
│                                      │
│  检测到: Linux amd64               │
│                                      │
│  [🚀 一键安装核心]                   │
│                                      │
│  安装过程需要管理员权限               │
│  （如需密码会弹窗提示）               │
└──────────────────────────────────────┘
```

### 2.3 模块页面

每个模块的 ZIP 包包含两个文件：

- **`{模块名}.so`**：模块二进制
- **`{模块名}.so.json`**：模块 manifest（含 UI 声明）

UI 根据 manifest 中的 `ui` 字段**动态渲染**模块页面，无需为每个模块单独编写 UI 代码。

详见 [SPEC-Impl.md] 模块 UI 声明规范。

### 2.4 设置页面

**UI 信息区：**
- 版本号
- 平台（如 `linux`）
- 架构标识（如 `amd64`）

**外观设置：**

三种主题模式，显示顺序固定：

| 顺序 | 模式 | 说明 |
|------|------|------|
| 1 | 系统默认 | 跟随操作系统设置，安装后默认模式 |
| 2 | 亮色模式 | 强制亮色 |
| 3 | 暗色模式 | 强制暗色 |

---

## 3. UI 与 Core 通信设计

### 3.1 为什么用 WebSocket

- **全平台通用：** Android / iOS / Desktop / Web，一个协议打天下
- **双向实时推送：** Server → Client / Client → Server 同时支持，无需轮询
- **HTTP/1.1 兼容：** 天然穿透企业代理，兼容现有网络环境

WebSocket 是 UI 与 Core 之间通信的**最小公分母**。

### 3.2 传输与序列化

| 项目 | 选择 |
|------|------|
| 传输层 | WebSocket（ws://127.0.0.1:19001/ws）|
| 序列化 | JSON |
| 心跳 | 每 30 秒 ping，60 秒无响应判定断开 |
| 重连 | 指数退避（1s → 2s → 4s → max 30s）|
| 请求超时 | 5 秒 |

---

## 4. Core 设计

### 4.1 定位

Core = **无头守护进程框架**，类比 Redis / cURL / 无头浏览器的设计理念——无 UI，纯 API/CLI，程序化控制。

### 4.2 职责

- 连接管理（UI ↔ Core WebSocket）
- 路由（模块间通信中介）
- 模块调度（加载/卸载/生命周期）
- 配置管理（持久化 + 热更新）

### 4.3 为什么用 Rust

| 理由 | 说明 |
|------|------|
| 性能优先 | 与 C/C++ 同级，适合网络 I/O 密集场景 |
| 内存安全 | 无 GC，无数据竞争，无 use-after-free |
| 跨平台 FFI | 与模块系统天然契合 |
| 现代包管理 | Cargo 一站式依赖与构建 |

---

## 5. Modules 与 Core 的关系

### 5.1 模块 = Core 的扩展功能

Modules 不是独立工作的实体，而是 Core 的**可插拔扩展**。

类比：模块像身体器官，大脑（Core）统一指挥，各器官只响应大脑的调度，不互相控制。

### 5.2 模块与 Core 的通信方式

- **模块 → Core**：模块向 Core 注册自己的能力（init），响应 Core 的调用（call）
- **Core → 模块**：Core 通过统一接口调用模块的方法，模块返回结果
- **模块间**：**不通信**，所有跨模块的业务由 Core 协调

```
同步层模块 → Core（请求发数据）
              Core → 通信层模块（实际发包）
                    对方 → Core → 同步层模块（收到数据）
```

模块专注于自己的功能，跨模块协作由 Core 统一调度。

---

## 6. Modules 设计

### 6.1 模块层次分类

#### 通信层 Modules（地基/桥梁）

| ID | 名称 | 开源许可 | 底层实现 | 说明 |
|----|------|---------|---------|------|
| `p2p` | P2P 网络 | AGPL-3.0 | libp2p | 去中心化节点发现与连接 |
| `tor` | TOR 匿名网络 | AGPL-3.0 | arti | 洋葱路由匿名流量 |
| `i2p` | I2P 隧道 | 待定 | 待定 | 匿名网络隧道（规划中） |

#### 加密层 Modules（装修/安全设施）

| ID | 名称 | 开源许可 | 说明 |
|----|------|---------|------|
| `crypto` | 加密管道 | 商业授权 | 端到端加密，可选高级模块 |

#### 同步层 Modules（人工作/车通行）

| ID | 名称 | 开源许可 | 底层实现 | 说明 |
|----|------|---------|---------|------|
| `crdt` | 文档同步 | AGPL-3.0 | yrs | 最终一致性文档协同 |
| `storage` | 存储管理 | AGPL-3.0 | 待定 | 本地持久化存储抽象 |

> **注：** 模块按需安装，不强制依赖。Core 仅声明最小能力集，其余均为可插拔模块。

### 6.2 模块分发格式

每个模块 ZIP 包包含：
- **`{模块名}.so`**：二进制插件
- **`{模块名}.so.json`**：manifest（包含 UI 声明）

安装目录：`{Core根目录}/Cloudfin/modules/`

### 6.3 模块配置流程

```
用户选择模块
    ↓
Core 获取模块 manifest，透传给 UI
    ↓
UI 根据 manifest 动态渲染配置面板
    ↓
用户填写配置 → Core 保存 → 模块生效
```

---

## 7. 其他设计

### 7.1 安装引导流程

```
第一步：安装 UI（用户自行完成）
         ↓
第二步：UI 检测 Core 状态
         │
         ├── 未安装 → 显示两个安装渠道选择
         │              ↓
         │    ┌─────────────────┬─────────────────┐
         │    │ 从 GitHub 安装  │ 从 Gitee 安装   │
         │    └─────────────────┴─────────────────┘
         │              ↓
         │         用户选择渠道
         │              ↓
         │         UI 检测 OS + 架构
         │              ↓
         │         匹配对应版本
         │              ↓
         │         自动下载 + 安装
         │         （Linux/macOS 需要 root 时弹窗输入密码，用完即删）
         │              ↓
         └── 已安装 → 继续
         ↓
第三步：UI 引导用户安装模块（模块市场）
         ↓
第四步：配置模块 → 连接节点 → 开始使用
```

### 7.2 安全设计原则

| 原则 | 实现方式 |
|------|---------|
| 模块权限显式授予 | Core 维护权限白名单，模块只能访问授权范围内的 API |
| UI ↔ Core 本地通信 | WebSocket 走本地回环（127.0.0.1 / localhost），不暴露公网端口 |
| P2P 流量端到端加密 | 默认使用 WireGuard 协议或 Noise 框架加密 |
| 敏感配置不离 Core | 私钥等敏感数据由外部密钥管理器处理，Core 只持有引用，不存明文 |

### 7.3 版本与兼容性

- **Core API 版本号**独立演进，每次 breaking change 递增主版本号
- **模块声明所依赖的 Core API 版本**，manifest 中明确声明 `core_api_version`
- **Core 拒绝加载不兼容的模块**：版本校验失败时模块不被加载，并返回明确错误信息
- **向后兼容**：Core 保持对同一主版本号内所有次版本号的兼容性

```
模块 manifest 字段：
  core_api_version: "1.2"   # 表示该模块依赖 Core API 1.2
```

### 7.4 版本命名规范

#### UI 版本格式

```
Cloudfin-UI-{系统}-{架构}-v{年}{月}{日}-{当日迭代序号}.{包格式}
```

| 字段 | 说明 |
|------|------|
| 系统 | `Android` / `Linux` / `Windows` |
| 架构 | `arm64-v8a` / `arm64` / `amd64` |
| 当日迭代序号 | 三位数字，从 001 开始，每日重置 |
| 包格式 | Android: `apk` / Linux: `tar.gz` / `deb` / `AppImage` / Windows: `exe` |

示例：
```
Cloudfin-UI-Android-arm64-v8a-v20260405-001.apk
Cloudfin-UI-Linux-amd64-v20260405-001.tar.gz
Cloudfin-UI-Windows-amd64-v20260405-001.exe
```

#### Core 版本格式

```
Cloudfin-Core-{系统}-{架构}-v{年}{月}{日}-{当日迭代序号}.{包格式}
```

| 字段 | 说明 |
|------|------|
| 系统 | `Android` / `Linux` / `Windows` |
| 架构 | `arm64-v8a` / `arm64` / `amd64` |
| 当日迭代序号 | 三位数字，从 001 开始，每日重置 |
| 包格式 | Android: `apk` / Linux: `tar.gz` / Windows: `exe` |

示例：
```
Cloudfin-Core-Android-arm64-v8a-v20260405-001.apk
Cloudfin-Core-Linux-amd64-v20260405-001.tar.gz
Cloudfin-Core-Windows-amd64-v20260405-001.exe
```

#### Modules 版本格式

```
Cloudfin-Module-{模块类型英文}-{系统}-{模块名}-v{年}{月}{日}-{当日迭代序号}.zip
```

| 字段 | 说明 |
|------|------|
| 模块类型英文 | `Network`（通信层）/ `Encrypt`（加密层）/ `Sync`（同步层）|
| 系统 | `Linux`（Android 和 Linux 共用）/ `Windows` / `MacOS`（macOS 和 iOS 共用）|
| 模块名 | 具体模块名，如 `P2P` / `CRDT` / `TOR` / `crypto` |
| 当日迭代序号 | 三位数字，从 001 开始，每日重置 |

示例：
```
Cloudfin-Module-Network-Linux-P2P-v20260405-001.zip
Cloudfin-Module-Encrypt-Windows-crypto-v20260405-001.zip
Cloudfin-Module-Sync-Linux-CRDT-v20260405-001.zip
Cloudfin-Module-Sync-MacOS-Storage-v20260405-001.zip
```

> **注意：** Modules 系统分组中，`Linux` 同时适用于 Android 和 Linux，`MacOS` 同时适用于 macOS 和 iOS。

#### 架构标识（通用）

| CPU 架构 | 标识 | 说明 |
|---------|------|------|
| x86_64 | `amd64` | 桌面/服务器主流 |
| arm64 / aarch64 | `arm64` | Apple Silicon / 服务器 |
| arm64-v8a（Android） | `arm64-v8a` | 安卓完整架构标识 |

---

*本文档为 L0 级别，所有 L1（模块级）/ L2（实现级）文档以此为准。*
